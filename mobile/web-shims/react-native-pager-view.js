import React from "react";

// Web-only stub: renders nothing. The reader screen that uses this component is
// not web-ready; this keeps it importable so the rest of the app can bundle.
const PagerView = React.forwardRef(function PagerViewStub(_props, _ref) {
  return null;
});

export default PagerView;
