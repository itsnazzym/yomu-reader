# Expo & Metro
.expo/
.metro/
metro-cache/
expo-env.d.ts

# Native projects & build outputs
node_modules/
android/
ios/
dist/
*.apk
*.aab

# Local environment & secrets
.env
.env.*.local
# mobile/keys/release.properties — mots de passe (voir scripts/release.properties.example)

# Test & bundle artifacts
screen_*.png
test-bundle.js
test/*.test.cjs

# Production signing keystore (NEVER commit)
keys/
*.keystore
*.jks

